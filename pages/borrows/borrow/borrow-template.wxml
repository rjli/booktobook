<template name="borrowTemplate">
  <view class="book-container" catchtap='onBorrowTap' data-borrow-id="{{id}}">
    <image class="book-img" src="{{image}}"></image>
    <text class="book-name">{{bookName}}</text>
  </view>
</template>